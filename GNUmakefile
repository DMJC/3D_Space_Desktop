include $(GNUSTEP_MAKEFILES)/common.make

APP_NAME = SceneViewer

SceneViewer_OBJC_FILES = main.m
SceneViewer_RESOURCE_FILES = scene.txt

ADDITIONAL_GUI_LIBS += -lGL -lm

include $(GNUSTEP_MAKEFILES)/application.make
